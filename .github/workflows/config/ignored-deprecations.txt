# Add deprecations that should be ignored when checking for deprecations to this file.
# Use the substrings/patterns that is printed to the compiler warning, one per line.
# E.g.: 'getBytes(int,int,byte[],int) in java.lang.String'
